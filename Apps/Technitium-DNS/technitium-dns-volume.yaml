apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-config
  namespace: dns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi