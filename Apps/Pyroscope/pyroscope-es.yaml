apiVersion: external-secrets.io/v1 
kind: ExternalSecret
metadata:
  name: pyroscope-s3-credentials
  namespace: pyroscope
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: pyroscope-s3-credentials
  data:
  - secretKey: AWS_ACCESS_KEY_ID
    remoteRef:
      key: secret/pyroscope
      property: access-key
  - secretKey: AWS_SECRET_ACCESS_KEY
    remoteRef:
      key: secret/pyroscope
      property: secret-key