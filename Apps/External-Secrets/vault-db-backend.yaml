apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-db-backend
spec:
  provider:
    vault:
      server: http://vault.vault.svc:8200
      version: "v1"
      auth:
        kubernetes:
          mountPath: kubernetes
          role: eso-role
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets