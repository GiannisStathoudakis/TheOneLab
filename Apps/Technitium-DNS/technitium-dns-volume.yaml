apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-config
  namespace: dns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openebs-lvm
  resources:
    requests:
      storage: 1Gi